
Remaining features to implement:

*** Profile & Settings
- Transform the settings popup into a profile page and clean up residues
- Add authentication and registration with login (email) and password + the possibility to auth/register via Google / Facebook
- Connect to cloud providers like OneDrive or Google Drive to sync the journal file once exported
- Configure a fixed schedule for syncing the file from/to cloud (only diff, not the whole file, but there may be changes in previous entries)
- Configure Gemini API key to use AI features
- Delete account feature
- Make the profile/settings page richer

*** UI & Features
- Social Media like features (messaging, follow, likes, comments) on public thoughts
- Keeping a history of modifications on a thought and allow to show the previous versions?

- Configure the default visibility for each diary + remove the default global one from the settings

*** AI
- Guess the file format for the converter
- Suggest tags given the text written
- Auto tag entries with a number of max tags per entry to parameterize
- Fix writing errors and style
- Pass a thought to AI for analysis or further discussion
- Tone and mood of thoughts stats (analyzed by AI)
- Subjects discussed stats (analyzed by AI)

*** Book Converter
- Based on tags to create chapters
- Connect the entries to form a chapter
- Do the same for the rest of tags

*** Import / Export
- Import from a file will use the configured default visibility
- Export to a file needs to add a visibility field like --[public/private] just after the tags.
- Ability to choose whether to export the visibility field or not, by default set to false
- Ability to choose into which diary you import the file
