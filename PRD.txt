
Remaining features to implement:

*** UI Features
- Ability to connect to cloud providers like OneDrive or Google Drive to sync the journal file once exported
- Ability to configure a fixed schedule for syncing the file from/to cloud (only diff, not the whole file, but there may be changes in previous entries)
- Ability to configure Gemini API key to use AI features
- Keeping a history of modifications on a thought and allow to show the previous versions?
- Intro website to showcase the app features and link to login/sign up

*** Social features
- Feed that shows public entries from other users with infinite scroll and to preview your own public entries
- Allow users to follow other users
- Comments on public entries
- Likes for entries and comments
- Make messaging private between users
- Notifications for messages, comments, likes, new followers
- Leaderboard for most active users, most liked entries, most commented entries
- Badges for achievements and karma points
- Feature requests and voting system for new features
- Add a way to report a public thought (call for violence) and ban the user if needed
- The user is first prompted to make the thought private, then temporarily banned if they don't comply, then permanently banned after repeated offenses

*** AI
- Guess the file format for the converter
- Suggest tags given the text written
- Auto tag entries with a number of max tags per entry to parameterize
- Fix writing errors and style
- Pass a thought to AI for analysis or further discussion
- Tone and mood of thoughts stats (analyzed by AI)
- Subjects discussed stats (analyzed by AI)
- Personality analysis through the choice of words and subjects
- AI features should be locked behind a paywall with a free trial (except if own API toekn is provided)
- AI chats social features, like sharing a chat to your feed for your followers to see and interact with (a chat that starts from a public thought)

*** Book Converter
- Based on tags to create chapters
- Connect the entries to form a chapter
- Do the same for the rest of tags

*** Import / Export
- Import from a file will use the configured default visibility of the diary
- The filename of the export is the name of the diary + the date of the export
- Ability to choose whether to export the visibility field or not, by default set to false
- Export to a file needs to add a visibility field like --[public/private] just after the tags.
- Manage the behavior of the import/export when using All Diaries

*** Security
- Add 2FA authentication
- Allow users to see and manage active sessions (logout from other sessions)
- Spam and bots protection on sign up and login forms
- Allow users to download their data (GDPR)

*** Technical improvements
- Add e2e tests (that open urls and perform actions) for critical user flows (sign up, login, create entry, edit entry, delete entry, export file, import file)
- Refactor codebase to use modern React 19 features (hooks, context API)
- After saving profile settings, it should not go back to entries, it should stay on profile page (and show a success toast)

- Setting up the infrastructure for deploying the application
- Dockerfiles for client and server
- Kubernetes deployment scripts
- CI/CD pipeline for automated testing and deployment

- Offline mode for journaling without internet connection
- Mobile app version for iOS and Android

- Optimize database for better performance
- Caching of frequent requests (public entries, feed)
- Set up monitoring and alerting for server health and performance
- Backup and disaster recovery plan for user data
- Add logging for critical actions and errors for better debugging
- Improve error handling and user feedback for better UX
- Use WebSockets for real-time updates on public entries and messages
- Implement lazy loading for components and routes to improve initial load time
- Implement feature flags to enable/disable features without redeploying
- Calibrate existing rate limiting on API endpoints to prevent abuse without being too restrictive
- Documentation for API endpoints and developer guides
