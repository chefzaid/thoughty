# ===========================================
# Thoughty Environment Configuration
# ===========================================
# IMPORTANT: Never commit the actual .env file to version control!
# Copy this file to .env and fill in your values.

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-strong-password-here
POSTGRES_DB=journal
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ===========================================
# JWT Authentication (CRITICAL - Change these!)
# ===========================================
# Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-64-chars-change-in-production
REFRESH_SECRET=your-super-secret-refresh-key-min-64-chars-change-in-production

# ===========================================
# Application Settings
# ===========================================
NODE_ENV=development
PORT=3001

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# ===========================================
# Email Configuration (Optional - for password reset)
# ===========================================
# For Gmail: Enable 2FA and create an App Password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="Thoughty <your-email@gmail.com>"

# ===========================================
# OAuth Configuration (Optional)
# ===========================================
# Google OAuth - https://console.developers.google.com/
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ===========================================
# Security Settings
# ===========================================
# Rate limiting is enabled by default
# Adjust these values based on your needs (in production environment variables)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# AUTH_RATE_LIMIT_MAX=5