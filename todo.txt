
*** Ongoing
profile with full name and photo with a settings buttons
settings for theme (dark/light) and path to txt file
date picker for dates but also support manual date input
tags should show a pick list of all available tags
10 thoughts per page
when there are many thoughts on the same day they should be ass follow ---2--[tags] ---3--[tags] and so on
thoughts are separated by 20 dashes except if the same day then they are separated by 20 stars

*** Next
app name : Thoughty, with a simple logo of someone daydreaming, with icon and thumbnail and titlebar
in settings we also select how many thoughts per page to display
remove photo upload and display for now
settings should be saved in a json file
interface language (english, french, ...)
tags are separated by commas but without a space after
if "entry (yyyy.mm.dd)" or "entry (yyyy.mm.dd)--X" then it should link to that thought (X being the thought number per day if there were many)
photo is to be uploaded
dark light theme switch not selection
date is by default today
date picker for date filter too (and manual entry too)
tags show a selection list with the ability to add new ones
path to text file as browse to file

display counts of thoughts per month/year
possibility to select pages (textbox)
ability to go the first thought of year or year/month
for date filter only allow/display dates where there is an actual thought

make a complete readme file with details of the app and how to use it
make a complete mask file with shortucts commands to run the app easily

use a DB instead of file and be able to export it to a text file
make a parser that would fill the DB from the text file
the parser could be further customized to support other formats that you configure in the settings

connect to cloud providers like OneDrive or Google Drive to sync the journal file

*** Future
use AI to autocomplete tags given the text written
use AI to fix writing errors and style
ability to pass a thought to AI for analysis or discussion